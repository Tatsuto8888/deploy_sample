<h1 class="text-2xl font-bold text-center mb-6">貯金登録をしよう！</h1>

<div class="max-w-lg mx-auto p-4 bg-white shadow-md rounded-lg">
  <%= form_with model: @board, local: true do |f| %>
    <!-- 貯金額（文字で保存） -->
    <div class="mb-4">
      <%= f.label :amount, "貯金額", class: "block font-medium" %><br>
      <%= f.select :amount, options_for_select([
        "1円以上",
        "5円以上",
        "10円以上",
        "50円以上",
        "100円以上",
        "500円以上",
        "1000円以上",
        "5000円以上",
        "10000円以上"
      ]), { prompt: "選択してください" }, class: "border p-2 rounded w-full" %>
    </div>

    <div class="mb-4">
      <label for="tag_names" class="block font-medium">タグ</label>
      <%= text_field_tag :tag_names, '', placeholder: "例: 貯金, 習慣化", class: "w-full border p-2 rounded" %>
    </div>
  
    <!-- 本文 -->
    <div class="mb-4">
      <%= f.label :body, "感想", class: "block font-medium" %><br>
      <%= f.text_area :body, rows: 4, class: "w-full border p-2 rounded" %>
    </div>
  
    <!-- ボタンエリア -->
    <div class="flex justify-between">
      <%= link_to "キャンセル", boards_path, class: "bg-gray-300 hover:bg-gray-400 text-black px-4 py-2 rounded" %>
      <%= f.submit "投稿する", class: "bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded" %>
    </div>
  <% end %>
</div>

  